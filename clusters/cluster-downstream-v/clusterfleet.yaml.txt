apiVersion: fleet.cattle.io/v1alpha1
kind: ClusterFleet
metadata:
  name: downstream-v
spec:
  targets:
    - name: downstream-v-target
      clusterSelector:
        matchLabels:
          kubernetes.io/cluster-name: downstream-v
      gitRepos:
        - name: fleet-baseline
